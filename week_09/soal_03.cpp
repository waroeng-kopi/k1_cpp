// Programmer : Adam Rahmat Ilahi
// Monday, 3 January 2022
//
// soal:
//		Menentukan nilai X1 & X2 dari persamaan kuadran : 
//		AX^2 + BX + C = 0. 
//		Nilai A, B & C diinputkan
//

#include <iostream>
#include <cmath>

int input(){
;;;;int Input

;;;;std::cin >> Input
;;;;return 0
;
}

int main(void){
;;;;std::cout << "Program mencari nilai X1 & X2 dari persamaan kuadrat" << std::endl
;;;;std::cout << "AX^2 + BX + C = 0" << std::endl
	
;;;;int A, B, C, X1, X2
	
;;;;std::cout << "Input Nilai A = "
;;;;A = input()

;;;;std::cout << "Input NIlai B = "
;;;;B = input()

;;;;std::cout << "Input Nilai C = "
;;;;std::cin >> C

	// proses pow()
;;;;X1 = -B + sqrt((pow(B, 2)) - (4 * A * C) / (2 * A))
;
;;;;X2 = -B + sqrt((pow(B * B)) - (4 * A * C) / (2 * A))
;
}
